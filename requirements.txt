# Runtime: **bittensor neuron code** (`pip install -r requirements.txt` + `pip install -e .`).
# - **Validator** image: root `Dockerfile` (python slim) — only these lines + `numpy` via bittensor.
# - **Miner** image: `docker/subnet-miner/Dockerfile` (CUDA) also runs this file, then installs
#   OpenFly / torch / transformers in separate Dockerfile RUN steps (same pattern as parent
#   `docker/openfly-dashboard/Dockerfile`).
#
# Neuron code is Bittensor + stdlib + `template/` (urllib JSON miner, dendrite/weights). The
# OpenFly VLM imports happen **only** when `OPENFLY_SUBNET_MINER_MODEL=openfly` in `neurons/miner.py`.
#
# Other compose services **do not use this file**:
# - `docker/openfly-ue/Dockerfile` — UE + Xvfb (CUDA runtime image, no subnet Python deps).
# - `docker/assets-init/Dockerfile` — only `huggingface_hub` for downloads.
#
# Do **not** paste all of `OpenFly-Platform/requirements.txt` into this file: the CUDA **miner**
# Dockerfile installs that list in its own `RUN pip …` layer (mirroring the main repo dashboard image).
# This `requirements.txt` stays the small bittensor pin set shared with the slim validator image.
#
# SDK 9.7.x — aligned with corporate subtensor fork era (v9.7.1 was not published on public PyPI;
# use 9.7.0 here, or `pip install` from your fork / private index if you ship 9.7.1).
#
# Dev/tests: pip install -r requirements-dev.txt
# async-substrate-interface 2.x enforces cyscale vs scalecodec; bittensor 9.7.x still pins scalecodec.
async-substrate-interface>=1.0,<2
bittensor[cli]==9.7.0
# Pin CLI to the same minor as the SDK; bittensor[cli] only requires bittensor-cli>=9.0.2 and would otherwise float to latest.
bittensor-cli==9.7.0
# Pin: bittensor 9.7.x calls get_encrypted_commit without `hotkey`; newer bittensor-drand (1.x)
# requires hotkey and breaks set_weights / commit-reveal. Match upstream SDK’s paired version.
bittensor-drand==0.5.0
